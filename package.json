{
  "name": "omniscribe-arksentry-workspace",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @arksentry/core && npm run build --workspace arksentry && npm run build --workspace @arksentry/web",
    "test": "npm run test --workspace @arksentry/core"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
